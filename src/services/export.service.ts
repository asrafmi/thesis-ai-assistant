// BUSINESS LOGIC LAYER — pure TypeScript functions. No React, no Next.js.

import type { SectionTree, Thesis } from '@/types/thesis.types'

export function buildDocxStructure(thesis: Thesis, sections: SectionTree[]) {
  // TODO: convert sections to docx-compatible structure
  return []
}
